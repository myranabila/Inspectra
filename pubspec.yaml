name: projectws
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # General UI
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

  # Media
  file_picker: ^8.0.0 # Kept the higher version and removed the duplicate

  # Firebase (harmonised on core 3.1.x)
  firebase_core: ^3.15.2
  firebase_auth: ^5.1.0 
  cloud_firestore: ^5.0.1
  firebase_storage: ^12.0.1 

  # State & Routing
  provider: ^6.1.5+1
  go_router: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true